[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "viewbase"
version = "3.0.0.dev0"
description = "Plochy a okna v prohlizeci jako knihovna: Instance, Screen, Window."
requires-python = ">=3.12"
license = { file = "../LICENSE" }
authors = [{ name = "Jindrich Nemec" }]
dependencies = [
    # TOTP a QR nejsou volitelny extra: bez nich se nedá zalozit identita
    # a do instance se nikdo neprihlasi (D-11).
    "pyotp>=2.9",
    "qrcode>=7.4",
]

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.hatch.build.targets.wheel]
packages = ["viewbase"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q --strict-markers"
